name: Borsa Bot

on:
  schedule:
    - cron: '0 6 */3 * *'
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest

    permissions:
      contents: write   # 🔥 Performance DB push için gerekli

    steps:
      - name: Repo çek
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Python kur
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Bağımlılıkları yükle
        run: |
          pip install -r requirements.txt

      - name: Botu çalıştır
        env:
          # Mevcut anahtarlar
          NEWS_API_KEY: ${{ secrets.NEWS_API_KEY }}
          SENDGRID_API_KEY: ${{ secrets.SENDGRID_API_KEY }}
          # Mail yapılandırması (ahm.cagil@hotmail.com ve ahm.cagil@gmail.com için)
          MAIL_SENDER: ${{ secrets.MAIL_SENDER }}
          MAIL_PASSWORD: ${{ secrets.MAIL_PASSWORD }}
          MAIL_RECIPIENT: ${{ secrets.MAIL_RECIPIENT }}
        run: |
          python main_bot.py

      - name: Performance DB Commit
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

          # Eğer dosya mevcutsa ekle
          if [ -f performance.db ]; then
            git add performance.db
            git diff --cached --quiet || git commit -m "📊 Performans veritabanı güncellendi: $(date +'%Y-%m-%d %H:%M')"
            git push
          else
            echo "performance.db bulunamadı, commit atlanıyor."
          fi
